mutation createPrintSetting($input: createPrintSettingInput!) {
    createPrintSetting(input: $input) {
        id
        dispensaryId
        printType
        isEnabled
        dimensionWidth
        dimensionHeight
        marginTop
        marginBottom
        marginLeft
        marginRight
        fontSize
        fontFamily
        topText
        bottomText
        createdAt
        updatedAt
    }
}
