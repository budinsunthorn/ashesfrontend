import { Metadata } from 'next';
import PageContent from './content';
export const metadata: Metadata = {
    title: 'Register Dispensaries',
};
const Dispensaries = () => {
    return <PageContent />;
};

export default Dispensaries;
