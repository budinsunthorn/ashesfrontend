import { Metadata } from 'next';
import PageContent from './content';
export const metadata: Metadata = {
    title: 'Metrc Item Category',
};
const ItemCategory = () => {
    return <PageContent />;
};

export default ItemCategory;
