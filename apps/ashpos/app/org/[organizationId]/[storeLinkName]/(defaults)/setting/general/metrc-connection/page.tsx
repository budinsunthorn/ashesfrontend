import PageContent from './content';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Metrc Connection',
};

const MetrcConnection = () => {
    return <PageContent />;
};

export default MetrcConnection;
