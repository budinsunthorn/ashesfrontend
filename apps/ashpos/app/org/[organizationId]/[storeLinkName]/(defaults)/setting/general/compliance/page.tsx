import PageContent from './content';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Compliance',
};

const MetrcConnection = () => {
    return <PageContent />;
};

export default MetrcConnection;
